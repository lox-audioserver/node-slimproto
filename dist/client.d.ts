import { EventEmitter } from "events";
import net from "net";
import { Buffer } from "node:buffer";
import { EventType, MediaDetails, MediaMetadata, PlayerState, TransitionType } from "./models.js";
import { SlimProtoVolume } from "./volume.js";
export interface SlimClientOptions {
    onEvent?: (event: EventType, data?: unknown) => void;
}
export declare class SlimClient extends EventEmitter {
    readonly socket: net.Socket;
    private readonly onEvent?;
    private buffer;
    private _connected;
    private heartbeatTimer?;
    private lastHeartbeat;
    private _playerId;
    private _deviceType;
    private _deviceName;
    private _capabilities;
    private _powered;
    private _muted;
    private _state;
    private _jiffies;
    private _lastTimestamp;
    private _elapsedMilliseconds;
    private _currentMedia?;
    private _bufferingMedia?;
    private _nextMedia?;
    private _autoPlay;
    private _presets;
    volumeControl: SlimProtoVolume;
    readonly extraData: Record<string, unknown>;
    constructor(socket: net.Socket, options?: SlimClientOptions);
    get connected(): boolean;
    get playerId(): string;
    get deviceType(): string;
    get deviceModel(): string;
    get firmware(): string;
    get maxSampleRate(): number;
    get supportedCodecs(): string[];
    get deviceAddress(): string;
    get name(): string;
    get volumeLevel(): number;
    get powered(): boolean;
    get muted(): boolean;
    get state(): PlayerState;
    get elapsedMilliseconds(): number;
    get elapsedSeconds(): number;
    get jiffies(): number;
    get currentUrl(): string | undefined;
    get currentMedia(): MediaDetails | undefined;
    get nextMedia(): MediaDetails | undefined;
    get presets(): MediaDetails[];
    set presets(presets: MediaDetails[]);
    disconnect(): void;
    private handleDisconnect;
    private handleData;
    private dispatchCommand;
    stop(): Promise<void>;
    play(): Promise<void>;
    pause(): Promise<void>;
    togglePause(): Promise<void>;
    power(powered?: boolean): Promise<void>;
    togglePower(): Promise<void>;
    volumeSet(volume: number): Promise<void>;
    volumeUp(): Promise<void>;
    volumeDown(): Promise<void>;
    mute(muted?: boolean): Promise<void>;
    next(): Promise<void>;
    playUrl(opts: {
        url: string;
        mimeType?: string | null;
        metadata?: MediaMetadata | null;
        transition?: TransitionType;
        transitionDuration?: number;
        enqueue?: boolean;
        autostart?: boolean;
        sendFlush?: boolean;
    }): Promise<void>;
    pauseFor(millis: number): Promise<void>;
    skipOver(millis: number): Promise<void>;
    unpauseAt(timestamp: number): Promise<void>;
    signalUpdate(): void;
    sendFrame(command: Buffer, data: Buffer): Promise<void>;
    private _renderDisplay;
    private _sendHeartbeat;
    private _sendStrm;
    private _process_helo;
    private _process_butn;
    private _process_knob;
    private _process_ir;
    private _process_dsco;
    private _process_stat;
    private _process_stat_stmc;
    private _process_stat_stmd;
    private _process_stat_stmf;
    private _process_stat_stmo;
    private _process_stat_stmp;
    private _process_stat_stmr;
    private _process_stat_stms;
    private _process_stat_stmt;
    private _process_stat_stmu;
    private _process_stat_stml;
    private _process_stat_stmn;
    private _process_resp;
    private _process_setd;
    private _parseCodc;
    private emitEvent;
}
